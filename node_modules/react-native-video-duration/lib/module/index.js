import { NativeModules, Platform } from 'react-native';
const VideoDuration = Platform.OS === 'android' ? NativeModules.VideoDurationModule : NativeModules.VideoDuration;
export async function getVideoDuration(videoPath) {
  const result = await VideoDuration.getVideoDuration(videoPath);
  if (result && result > 0) {
    return Platform.OS === 'android' ? result / 1000 : result;
  }
  return 0;
}
//# sourceMappingURL=index.js.map